# GGUF backend requirements (vision-capable llama-cpp-python).
# Follow the platform-specific install guide:
# docs/LLAMA_CPP_PYTHON_VISION_INSTALL.md
#
# Linux (CUDA) example:
# pip install --upgrade --force-reinstall --no-cache-dir \
#   llama-cpp-python==<version> --extra-index-url <vision-wheel-index>
#
# Windows example:
# 1) Install a vision-capable wheel from the guide.
# 2) Then verify with:
#    python -c "import llama_cpp; print(llama_cpp.__version__)"
#
# Supported chat handlers (auto-selected by model filename):
#   - Qwen3VLChatHandler / Qwen25VLChatHandler  -> Qwen-VL GGUFs
#   - Gemma4ChatHandler                         -> Gemma 4 GGUFs
#
# Gemma 4 support requires the JamePeng fork of llama-cpp-python v0.3.35 or later.
# See the install guide above (docs/LLAMA_CPP_PYTHON_VISION_INSTALL.md) for the
# fork's discussion thread and wheel locations.
# Earlier versions lack Gemma4ChatHandler; loading a Gemma GGUF will raise
# ImportError from AILab_QwenVL_GGUF.py._load_model().
#
# Placeholder only; do not rely on this line alone:
# llama-cpp-python
